{
  "name": "verify-gate-revert",
  "category": "verify_gate",
  "note": "A verification-blocked workspace draft is retained for explicit recovery instead of being silently reverted. The failed terminal and blocked action preserve the failure while the draft remains inspectable.",
  "prompt": "make config.txt valid",
  "tools": true,
  "max_steps": 8,
  "timeout_ms": 60000,
  "verify": {
    "mode": "hard",
    "scope": "repository",
    "on_failure": "revert",
    "command": "grep -q '^mode = safe$' config.txt",
    "max_repair_steps": 0
  },
  "expect": {
    "terminal": "failed",
    "files": {
      "config.txt": "mode = unsafe\n"
    },
    "tools_used": ["file_read", "file_edit"],
    "verify_status": "failed",
    "verify_action": "blocked",
    "verify_repairs": 0
  }
}
