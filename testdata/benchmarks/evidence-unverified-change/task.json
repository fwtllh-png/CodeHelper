{
  "name": "evidence-unverified-change",
  "category": "context_assembly",
  "note": "With the gate off nothing proves the edit, so the retained draft must not publish as a completed workspace change. The risk remains visible in the next sample and receipt while the fixture workspace returns to its seed.",
  "prompt": "raise the timeout in server.py to 30",
  "tools": true,
  "max_steps": 6,
  "timeout_ms": 60000,
  "expect": {
    "terminal": "failed",
    "unchanged": ["server.py"],
    "tools_used": ["file_read", "file_edit"],
    "receipt_changes": ["server.py"],
    "verify_status": "unavailable",
    "verify_action": "repair",
    "context_sections": ["evidence"],
    "receipt_evidence_risks": ["changed_without_verification"],
    "receipt_not_collected_excludes": ["evidence"]
  }
}
